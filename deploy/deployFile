pipeline {
    agent any

    stages {
        stage('Deploy'){
            steps{
                bat 'docker run -d -p 8090:8090 myapps/mycycleapp:latest'
            }
        }
    }

    post {
        always {
            bat 'docker image ls'
            bat 'docker container ls'
        }
    }
}
